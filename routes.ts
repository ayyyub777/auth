export const publicRoutes = ["/", "/confirm-email"];

export const authRoutes = ["/signin", "/register", "/reset-password"];

export const authApi = "/api/auth";

export const postSigninRedirect = "/dashboard";
